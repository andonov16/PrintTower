<Window x:Class="Print_Tower.ExploreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Print_Tower"
        mc:Ignorable="d"
        Title="Print Tower" Height="400" Width="400">


    <Border Padding="5">
        <StackPanel>
            <TextBlock Name="DeviceName_Block" FontSize="26" Grid.Column="0"/>
            <TextBlock Text="Device name" Margin="0,0,0,20"/>
            <TextBlock Name="DeviceIP_Block" FontSize="26"/>
            <TextBlock Text="Device IP" Margin="0,0,0,20"/>

            <DataGrid Name="DevPropsGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Property name" Binding="{Binding Path=PropName}" Width="*"></DataGridTextColumn>
                    <DataGridTextColumn Header="Property OID" Width="*" Binding="{Binding Path=PropOID}" ></DataGridTextColumn>
                    <DataGridTextColumn Header="Property value" Width="*" Binding="{Binding Path=PropValue}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Border>
</Window>
